type UpdateMyProfileResponse {
    ok: Boolean!
    error: String
}

type Mutation {
    UpdateMyProfile(
        firstName: String
        lastName: String
        password: String
        profilePhoto: String
        age: Int
        ): UpdateMyProfileResponse!
}